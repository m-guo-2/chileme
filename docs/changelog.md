## [2026-03-08]

### 新增
- 新建 `Next.js + TypeScript` 全栈工程骨架，落地首页、记录、记忆、我的四个一级页面
- 新增 mock API、领域模型、规则分析器、记忆聚合器、推荐引擎、每周总结生成器
- 新增 TypeScript 语音输入 hook，浏览器支持时可直接进行语音转写

### 修改
- 将首页设计为围绕“记录这一顿”和“看看现在吃什么”的轻量闭环
- 将记录、记忆、推荐、历史统一到同一份 mock store 中，确保逻辑前后一致

### 验证
- `npm run lint`
- `npm run build`
